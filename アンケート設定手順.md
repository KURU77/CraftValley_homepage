# アンケート回答の保存設定（Googleフォーム）

サイトを開いたときに表示されるアンケートの回答を、
**Googleスプレッドシートに自動で貯める**ための設定手順です。

エンジニアでなくても作業できます。所要時間は**15分ほど**です。

> **現在の状態**：設定がまだ空のため、回答はどこにも保存されていません。
> 設定を入れるまでサイトは通常どおり動きます（エラーは出ません）。

---

## 全体の流れ

```
① Googleフォームを作る（7つの質問欄）
        ↓
② 「事前入力したURL」を取得する
        ↓
③ そのURLを index.html に貼り付ける
        ↓
④ GitHubにアップして完了
```

---

## ① Googleフォームを作る

1. <https://forms.google.com> を開き、**「空白のフォーム」**をクリック
2. フォーム名を「**Craft Valley アンケート回答**」などに変更
3. 質問を**7つ**作ります。**すべて「記述式」**を選んでください

| # | 質問のタイトル | 内容 |
|---|---|---|
| 1 | `Q1` | クラフトバレーを知っていたか |
| 2 | `Q2` | 名前から浮かぶイメージ |
| 3 | `Q3` | 伝統工芸品の購入経験 |
| 4 | `Q4` | 最も楽しみにしていること |
| 5 | `Q5` | 自由記述 |
| 6 | `STATUS` | 完了／スキップ／離脱 |
| 7 | `LANG` | 表示言語（en / ja） |

> **タイトルは必ず上の表のとおり**（半角大文字）にしてください。
> この文字を目印にして、サイト側が送り先を判別します。
> 質問の説明文は自由に書いて構いません。

4. 「必須」のスイッチは**すべてオフ**のままにします
   （時間切れで空欄のまま送信されることがあるため）

---

## ② 「事前入力したURL」を取得する

1. フォーム編集画面の**右上の「⋮」**（縦三点）をクリック
2. **「事前入力したURLを取得」**を選ぶ
3. プレビュー画面が開くので、**各欄に、その質問のタイトルとまったく同じ文字**を入力します

   | 質問 | 入力する文字 |
   |---|---|
   | Q1 の欄 | `Q1` |
   | Q2 の欄 | `Q2` |
   | Q3 の欄 | `Q3` |
   | Q4 の欄 | `Q4` |
   | Q5 の欄 | `Q5` |
   | STATUS の欄 | `STATUS` |
   | LANG の欄 | `LANG` |

4. 一番下の**「リンクを取得」**をクリック
5. 画面の下に出る**「リンクをコピー」**をクリック

これで、次のような長いURLがコピーされます。

```
https://docs.google.com/forms/d/e/1FAIpQLSxxxxxxxx/viewform?usp=pp_url
&entry.111111=Q1&entry.222222=Q2&entry.333333=Q3&entry.444444=Q4
&entry.555555=Q5&entry.666666=STATUS&entry.777777=LANG
```

> このURLの中に、**送信先とすべての項目IDが含まれています**。
> 数字を1つずつ調べる必要はありません。

---

## ③ index.html に貼り付ける

1. `index.html` をテキストエディタで開く
2. **`prefilledUrl`** という文字を検索する（400行目あたり）
3. `""` の**中に**、コピーしたURLを貼り付ける

**変更前**
```javascript
var SURVEY_FORM = {
  prefilledUrl: ""
};
```

**変更後**
```javascript
var SURVEY_FORM = {
  prefilledUrl: "https://docs.google.com/forms/d/e/1FAIpQLSxxxx/viewform?usp=pp_url&entry.111111=Q1&..."
};
```

> ⚠️ **前後の `"` を消さないでください。** URLは1行で貼り付けます。

4. 保存する

---

## ④ 動作を確認する

1. `index.html` をブラウザで開く
2. アンケートに最後まで答える
3. Googleフォームの**「回答」**タブを開き、1件増えていれば成功です

回答が届かないときは、下の「うまくいかないとき」を参照してください。

---

## ⑤ スプレッドシートに貯める

1. フォームの**「回答」**タブを開く
2. 右上の**緑色のスプレッドシートのアイコン**をクリック
3. 「新しいスプレッドシートを作成」→**「作成」**

以降、回答は自動でスプレッドシートに追加されます。
グラフ化や絞り込みも自由にできます。

---

## 記録される内容

| 項目 | 記録される値の例 |
|---|---|
| Q1 | `know-well` / `heard-of` / `first-time` / `not-sure` |
| Q2 | `traditional-craft` / `mountains` / `food-drink` / `modern-brand` / `no-image` |
| Q3 | `yes-several` / `yes-once` / `want-to` / `not-yet` |
| Q4 | `artisans` / `buy` / `story` / `food` / `visit` |
| Q5 | 訪問者が入力した自由記述 |
| STATUS | `完了` / `スキップ` / `離脱` |
| LANG | `en` / `ja` |

**STATUS の意味**

- **完了** … 最後まで回答した
- **スキップ** … 「Skip survey」を押して途中でやめた
- **離脱** … 回答の途中でページを閉じた（入力済みの分だけ記録）

> 個人を特定する情報（氏名・メールアドレス・IPアドレス）は**一切送信していません**。

---

## キャラクターのセリフを変える（編集画面から）

アンケートは、4人のドットキャラが順番に話しかける会話形式です。
**名前・セリフ・質問・選択肢・結びは、編集画面から直せます。**

1. `admin.html` を開き、上の **「🎭 アンケート」** を押す
2. 左のタブで直したい場所を選ぶ

   | タブ | 直せるもの |
   |---|---|
   | キャラクターの名前 | 4人＋「みんな」の名前 |
   | ボタン・共通の文言 | とばす／送る／時間切れ／扉が開いた など |
   | 場面 1〜5 | 登場のセリフ・質問文・各選択肢の文字と返事 |
   | 結びのひとこと | Q4の答えごとの最後の言葉 |

3. 日本語と英語を並べて書き換える（自動で下書き保存）
4. **「👁 プレビュー」** で実際の会話を確認（回答は送信されず、クッキーも残りません）
5. **「💾 survey.js を書き出す」** → ダウンロードされた `survey.js` を `shared/js/survey.js` に上書き
6. GitHubにアップロード

> **各選択肢の「記号」（`know-well` など）は集計に使うため、編集画面でも変更できないようにしてあります。**
> それ以外の文字は自由に直せます。
>
> 「登場のセリフ」は **1行が1回の表示** です。改行で区切ると、▼を押すごとに次の行が出ます。
> 質問文や結びの中で `<em>…</em>` で囲んだ部分は金色で強調されます。

| キャラ | 画像 | 担当する質問 |
|---|---|---|
| 1人目（案内人） | dot-character.png | Q1 知っていたか |
| 2人目 | dot-friend1.png | Q2 浮かぶイメージ |
| 3人目 | dot-friend2.png | Q3 工芸品の経験 |
| 4人目（旅人） | dot-friend3.png | Q4 探しているもの |
| 4人全員 | — | Q5 自由記述 |

---

## 知っておいていただきたいこと

**1問につき10秒の制限があります**

時間切れになると自動で次の質問へ進むため、
**空欄のまま記録される回答が一定数出ます**。これは仕様どおりの動作です。

空欄が多すぎると感じた場合は、制限時間を延ばせます。
`index.html` の以下の箇所を変更してください。

```javascript
var TIMER_DURATION = 10;   // ← この数字を 15 や 20 に変更
```

**同じ人は180日間、再表示されません**

一度回答（またはスキップ）した方には、約半年間アンケートが表示されません。
そのため、**回答数は訪問者数より少なくなります**。

---

## うまくいかないとき

**回答がフォームに届かない**

- URLの前後の `"` が消えていないか確認してください
- 質問のタイトルが `Q1` `Q2` … `STATUS` `LANG` と**半角大文字で完全一致**しているか確認してください
- 事前入力URLを取得するとき、各欄に入力した文字が
  タイトルと**同じ**になっているか確認してください（`Q1` の欄に `Q1`）

**一部の項目だけ空になる**

- 制限時間（10秒）で自動送りされた分です。異常ではありません

**エラーが出ていないか確かめたい**

- ブラウザで <kbd>F12</kbd> を押し、「Console（コンソール）」タブを開きます
- 赤いエラーが出ていなければ、送信処理は正常です
  （送信結果自体はブラウザの仕様上、確認できません）

---

## 公開前に必要なこと

回答を集める場合、**プライバシーポリシーへの記載が必要**です。
以下のような一文を用意してください。

> 当サイトでは、サービス改善のため、初回訪問時のアンケート回答を
> 匿名で収集しています。氏名・連絡先などの個人情報は取得していません。

プライバシーポリシーのページは現在未作成です。
雛形が必要でしたらお知らせください。

---

*最終更新: 2026年8月*
